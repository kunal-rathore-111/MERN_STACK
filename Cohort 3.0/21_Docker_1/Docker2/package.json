{
  "name": "docker2",
  "module": "src/index.ts",
  "private": true,
  "scripts": {
    "dev": "bunx drizzle-kit generate && bunx drizzle-kit migrate &&  bun --watch ./src/index.ts",
    "build": "bunx drizzle-kit generate && bunx drizzle-kit migrate && tsc",
    "start": "bun ./dist/index.js"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/pg": "^8.20.0",
    "drizzle-kit": "^0.31.10"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@types/express": "^5.0.6",
    "drizzle-orm": "^0.45.2",
    "express": "^5.2.1",
    "pg": "^8.21.0"
  }
}
